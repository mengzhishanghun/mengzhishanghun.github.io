---
import Base from '../../../layouts/Base.astro';import Icon from '../../../components/Icon.astro';import ProductCard from '../../../components/ProductCard.astro';import{languages,products,ui,links,type Lang}from'../../../data/site';export function getStaticPaths(){return languages.map(lang=>({params:{lang}}));}const lang=Astro.params.lang as Lang;const t=ui[lang];const zh=lang==='zh';
---
<Base lang={lang} title={t.projects} area="projects"><section class="container page-intro"><p class="eyebrow">PRODUCTS & PROJECTS</p><h1>{zh?'实用工具，来自实际需求。':'Useful tools, built from real needs.'}</h1><p>{zh?'从 Unreal Engine 插件打包，到插件管理和 Markdown 整理。选择需要的工具，查看说明与获取方式。':'From Unreal Engine plugin packaging to plugin management and Markdown preparation. Explore the tools and their guides.'}</p></section><section class="container catalog-section"><div class="product-grid">{products.map(product=><ProductCard product={product} lang={lang}/>)}</div></section><section class="container store-strip"><div><p class="eyebrow">UNREAL ENGINE PLUGINS</p><h2>{zh?'更多商业插件，前往 Fab。':'More plugins on Fab.'}</h2><p>{zh?'网络通信、资产管理与开发辅助类插件。具体功能、版本和购买信息，以商店中的产品页面为准。':'Networking, asset management, and development utilities. Refer to each store listing for features, supported versions, and purchase information.'}</p></div><a class="button button-dark" href={links.fab} target="_blank" rel="noopener noreferrer">{zh?'浏览 Fab 商店':'Browse Fab'}<Icon name="external"/></a></section></Base>
