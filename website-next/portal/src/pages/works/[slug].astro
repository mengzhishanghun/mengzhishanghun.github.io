---
import { products } from '../../data/site';
export function getStaticPaths(){return products.map(product=>({params:{slug:product.id},props:{target:'/works/#'+product.id}}));}
const { target } = Astro.props;
return Astro.redirect(target);
---
