---
import { cases } from '../../data/site';
export function getStaticPaths(){return cases.map(item=>({params:{slug:item.id},props:{target:'/cases/#'+item.id}}));}
const { target } = Astro.props;
return Astro.redirect(target);
---
