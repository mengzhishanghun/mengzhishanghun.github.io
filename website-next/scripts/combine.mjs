import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';
const root=fs.realpathSync(path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'));const out=path.join(root,'preview.local');if(path.dirname(out)!==root||fs.existsSync(out)&&fs.lstatSync(out).isSymbolicLink())throw new Error('Unsafe preview output path');for(const app of['portal','docs'])if(!fs.existsSync(path.join(root,app,'dist','index.html')))throw new Error(app+' build output is missing');fs.rmSync(out,{recursive:true,force:true});fs.cpSync(path.join(root,'portal','dist'),out,{recursive:true});fs.cpSync(path.join(root,'docs','dist'),path.join(out,'docs'),{recursive:true});console.log('Combined local preview: '+out);
